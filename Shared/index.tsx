import Shared from "./Shared"
import {
  useFeatureDispatch,
  useFeatureSelect,
  selectShared,
  setSharedKey,
} from "./store/slice";
import { firstAction } from './store/actions/firstAction';

export {
  Shared,
  useFeatureDispatch,
  useFeatureSelect,
  selectShared,
  setSharedKey,
  firstAction,
};
